#!/bin/sh

echo "Running cargo audit before push..."
cd craft-nexus-contract
cargo audit
if [ $? -ne 0 ]; then
  echo "cargo audit failed! Please fix vulnerable dependencies before pushing."
  exit 1
fi
exit 0
